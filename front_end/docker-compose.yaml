services:
  trading-system:
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    command: ["npm", "run", "dev"]
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - BASE_URL=http://localhost:8000
