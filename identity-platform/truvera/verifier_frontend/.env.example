# Verifier Frontend Environment Configuration

# Backend API URL
VITE_API_URL=http://localhost:4001

# Truvera API URL (if using Truvera integration)
VITE_TRUVERA_API_URL=https://api-testnet.dock.io

# Custom integration settings
VITE_CUSTOM_INTEGRATION_ENABLED=false

# Default timeout for proof requests (in minutes)
VITE_DEFAULT_TIMEOUT_MINUTES=15
